# Accessibility

Every component must preserve native semantics where possible, support keyboard interaction, expose an accessible name when required, and pass automated accessibility checks before release.
