# Framework integrations

HANDHELD is framework-agnostic. `@handheld/components` is the canonical Web Components package. Framework packages only provide ergonomic integration; they must not duplicate component behavior.

- `@handheld/react`: typed JSX entry points
- `@handheld/vue`: Custom Elements compiler configuration
- `@handheld/angular`: Custom Elements schema integration
